import Button from "./button";
import Select from "./select";
import Stars from "./stars";
import Input from "./input";

export enum SizesEnum {
    SMALL = "small",
    MEDIUM = "medium",
    LARGE = "large"
}

export enum TypesEnum {
    PRIMARY = "primary",
    SECONDARY = "secondary"
}

export { Button, Select, Stars, Input };
